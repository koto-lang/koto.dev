# Fizz buzz in Koto

fizz_buzz = |n|
  match n % 3, n % 5
    0, 0 then "Fizz Buzz"
    0, _ then "Fizz"
    _, 0 then "Buzz"
    else n

play.setup = ||
  current_value: 0

play.set_fps 5
play.update = |state|
  n = state.current_value += 1
  print fizz_buzz n
