# Fizz buzz in Koto

fizz_buzz = |n|
  match n % 3, n % 5
    0, 0 then "Fizz Buzz"
    0, _ then "Fizz"
    _, 0 then "Buzz"
    else n

play.initial_state = ||
  update_time: 0
  n: 0

play.update = |state, time|
  if time >= state.update_time
    state.update_time = time + 0.2
    n = state.n += 1
    print fizz_buzz n
