# Fizz buzz in Koto

fizz_buzz = |n|
  match n % 3, n % 5
    0, 0 then "Fizz Buzz"
    0, _ then "Fizz"
    _, 0 then "Buzz"
    else n

play.setup = ||
  next_update_time: 0
  current_value: 0

play.update = |state, time|
  if time >= state.next_update_time
    state.next_update_time = time + 0.2
    n = state.current_value += 1
    print fizz_buzz n
